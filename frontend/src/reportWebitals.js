// frontend/src/reportWebVitals.js
import { reportWebVitals } from 'web-vitals';

// This is where the web vitals are logged. You can send them to an analytics endpoint or simply log them.
const reportWebVitals = (metric) => {
  console.log(metric); // You can change this to send the metrics to a backend or other analytics service
};

export default reportWebVitals;
